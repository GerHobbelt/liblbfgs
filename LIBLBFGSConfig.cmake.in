get_filename_component(LIBLBFGS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH )
get_filename_component(LIBLBFGS_INSTALL_PREFIX "${LIBLBFGS_CMAKE_DIR}" PATH)

SET(LIBLBFGS_INCLUDE_DIR "@LBFGS_INCLUDE_DIR_CONFIG@")
set(LIBLBFGS_LIBRARY_DIR "@LBFGS_LIBRARY_DIR_CONFIG@")
SET(LIBLBFGS_LIBRARY     "@LBFGS_LIBRARY_CONFIG@")

set(LIBLBFGS_FOUND TRUE )
